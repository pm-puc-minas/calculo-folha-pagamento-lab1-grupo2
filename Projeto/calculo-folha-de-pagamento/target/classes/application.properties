spring.application.name=calculo-folha-de-pagamento

# URL de conex�o para o banco de dados SQLite
# O caminho deve ser relativo a raiz do 'Projeto'
spring.datasource.url=jdbc:sqlite:rh_database.db

# Driver JDBC espec�fico para o SQLite
spring.datasource.driver-class-name=org.sqlite.JDBC

# Dialeto espec�fico para o Hibernate entender o SQLite
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect

# Ela for�a o Spring/Jackson a N�O serializar (escrever) datas como n�meros (timestamps).
spring.jackson.serialization.write-dates-as-timestamps=false

# Garante que o formato de data (para LocalDateTime) seja o padr�o ISO
spring.jackson.date-format=com.fasterxml.jackson.databind.util.ISO8601DateFormat

#Para ver o SQL gerado pelo Hibernate no console
spring.jpa.show-sql=true

# Impede o Hibernate de criar nomes "impl�citos" (ex: para tabelas de jun��o) de forma diferente.
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl

# Diz ao Hibernate o que fazer quando ligar a aplica��o
spring.jpa.hibernate.ddl-auto=update

# Configura a porta do servidor
server.port=9090

# User e Senha para acesar na aplica��o (Nao vamos usar na Sprint Dois)
spring.security.user.name=user
spring.security.user.password=senhaTeste

# H2 Config
# spring.jpa.hibernate.ddl-auto=create-drop

# For�a o Hibernate a usar os nomes f�sicos definidos nas anota��es @Table e @Column, em vez de sua estrat�gia padr�o
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl